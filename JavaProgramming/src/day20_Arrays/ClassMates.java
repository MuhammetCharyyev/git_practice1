package day20_Arrays;

import java.util.Arrays;

public class ClassMates {

    public static void main(String[] args) {

        String[] classmates = {"Jeff Bezos", "Elon Musk", "Bernard Arnault", "Bill Gates"};
       String[] initials = {"JF", "EM", "BA", "BG"};
      /*  int secondInitial = classmates.indexOf(" ");


        for (int j = 0; j < classmates.length; j++) { // outer loop to repeat inner loop for checking others indexes of array

            String firstInitial = classmates[0]; // checking 0 index
            String initials = "";

            for (int i = 0; i < classmates.length; i++) {
                if (classmates[i].equals(element)) {
                }
            }

       */




        System.out.println(Arrays.toString(classmates) +"\n" +Arrays.toString(initials));


    }


}
   /* create an array named classmates, and store 10 of your classmates' full names
        print the initials of each classmates in separate lines

    */
